export class LoginUsuario {
  nombreUsuario: string;
  passwords: string;
  constructor(nombreUsuario: string, passwords: string) {
      this.nombreUsuario=nombreUsuario;
      this.passwords=passwords;
  }
}
